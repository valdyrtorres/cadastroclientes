server:
  port: 8081
  servlet:
    context-path: /crud-clientes
    
spring: 
  application:
    name: crud
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect
  jmx:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3316/crud-clientes?allowPublicKeyRetrieval=true&sslMode=DISABLED&useSSL=false&serverTimezone=UTC
    username: valdir
    password: valdir123
